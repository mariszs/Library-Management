
Librarian (system, program)
Books
    see all books
    search book
        ? do we add more books to database?

Client
    see all clients
    search clients
        ? do we add more books to database?

Booking
    borrow book - join book id and client id
    add message after booking
    see all bookings
        ? return book?


